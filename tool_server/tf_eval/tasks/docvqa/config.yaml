task_name: "docvqa"
dataset_path: lmms-lab/DocVQA
# num_sample: 5349 # testset
# num_sample: 50
dataset_type: "unique" # Which means we need to implement load data logic sepretely for charxiv
generation_config:
  max_length: 2048
  # temperature: 0.6


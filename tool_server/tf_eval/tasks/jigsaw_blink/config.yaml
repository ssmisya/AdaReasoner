task_name: "jigsaw_blink"
dataset_path: "/mnt/petrelfs/songmingyang/songmingyang/data/vl_reasoning/tool_dataset/BLINK"
dataset_type: "unique"  # 自定义数据加载逻辑
split: ["test", "val"]  # 测试两个集合
num_sample: null  # 设为null以使用全部数据，或者指定一个整数以限制样本数量
generation_config:
  max_length: 2048
  temperature: 0.0  # 低温度使生成更加确定性
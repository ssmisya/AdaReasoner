task_name: "ocrbench"
dataset_path: /mnt/petrelfs/share_data/sunhaoyu/datasets/OCRBench
# num_sample: 1000 # 最大1000
# num_sample: 50
dataset_type: "unique" # Which means we need to implement load data logic sepretely for charxiv
generation_config:
  max_length: 2048


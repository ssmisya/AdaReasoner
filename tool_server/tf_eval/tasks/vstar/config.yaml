task_name: "vstar"
dataset_path: /mnt/petrelfs/sunhaoyu/visual-code/datasets/vstar_bench
num_sample: 191
dataset_type: "unique" # Which means we need to implement load data logic sepretely for charxiv
generation_config:
  max_length: 2048
  # temperature: 0.6


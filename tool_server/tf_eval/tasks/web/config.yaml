task_name: "web"
dataset_path:  /mnt/petrelfs/share_data/sunhaoyu/datasets/web_evaluation/GUIChat/sft_v1
# num_sample: 1100
dataset_type: "unique" # Which means we need to implement load data logic sepretely for charxiv
generation_config:
  max_length: 8192
  # temperature: 0.6

